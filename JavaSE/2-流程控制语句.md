# 流程控制语句

## 一、if

### 1、if-else

```java
if (boolean) {
    代码块
} else if (boolean) {
    代码块
} else if (boolean) {
    代码块
} else { // 默认情况
    代码块
}
```

### 2、三目运算符

**语法：**`boolean ? 数1 ： 数2`

执行过程：计算boolean的值

​               **若为true：**则整个结果为数1

​               **若为false：**则整个结果为数2

```java
	int max1 = num1 >= num2 ? num1 : num2;
    System.out.println(max1);
```

### 3、+号运算符

- 两边都是数字，则做的就是`加法运算`

- 若一边为字符串，则做的是`字符串的拼接`

```java
    public void test3 (){
            int age = 28;
            //豆豆加加
            System.out.println("我的年龄是23岁");
            System.out.println("我的年龄是" + age + "岁");
            String str = "" ;//空字符串
            //char ch = '';//没有空字符
            System.out.println(10 + 20 + "" + 30);//"3030"
            System.out.println("" + 10 + 20 + 30);//"102030"
    }
```

## 二、循环

### 1、While和For

任何复杂的程序逻辑都可以通过“顺序”、“分支”、“循环”这三种基本结构来实现；

循环的三要素是：`循环条件的初始化`、`循环条件的控制`、`循环条件的改变`。

**While条件循环**的用法：

```java
    public void test4(){
        int i = 1;
        while (i <= 5){
            System.out.println("HelloTinsur");
            i++;//执行下一次
        }
```

**For循环**的用法：

```java
	for(变量声明和赋值;循环条件;变量自加){
        循环体
    }
```

```java
    @Test
    public void test5(){
        for (int i = 1; i <= 5; i++) {
            System.out.println("HelloTinsur");
        }
    }
```



### 2、continue、break

**continue：**跳出本次`循环`，继续下一次循环；

**break：**跳出离他最近的那层`循环`。

### 3、双重For循环

**示例：**九九乘法表

```java
    @Test
    public void test2(){
        for (int i = 1; i <= 9; i++) {
            for (int j = 1; j <= i ; j++) {
                System.out.print(j + "*" + i + "=" + i*j + "\t");
            }
            System.out.println();
        }
    }
```

外层循环控制行数，数一下有几行就能确定外层循环；

内层循环控制列数，这一行打印多少个，到底要打印多少个要找出和当前行之间的一个关系。

**输出：**

```java
1*1=1	
1*2=2	2*2=4	
1*3=3	2*3=6	3*3=9	
1*4=4	2*4=8	3*4=12	4*4=16	
1*5=5	2*5=10	3*5=15	4*5=20	5*5=25	
1*6=6	2*6=12	3*6=18	4*6=24	5*6=30	6*6=36	
1*7=7	2*7=14	3*7=21	4*7=28	5*7=35	6*7=42	7*7=49	
1*8=8	2*8=16	3*8=24	4*8=32	5*8=40	6*8=48	7*8=56	8*8=64	
1*9=9	2*9=18	3*9=27	4*9=36	5*9=45	6*9=54	7*9=63	8*9=72	9*9=81	
```



## 三、分支判断

**switch：**可以接受的值`（byte、short、int、char、String和枚举）`不支持long。

示例：判断一个月有多少天

```java
    @Test
    public void test3(){
        //输入月份判断天数
        Scanner scanner = new Scanner(System.in);
        System.out.println("请输入月份：");
        int month = scanner.nextInt();
        switch (month){
            case 1:
            case 3:
            case 5:
            case 7:
            case 8:
            case 10:
            case 12:
                System.out.println("31天");
                break;
            case 4:
            case 6:
            case 9:
            case 11:
                System.out.println("30天");
                break;
            default:
                System.out.println("28天");
        }
    }
```





